sudo: false
language: ruby
cache: bundler
rvm:
- 2.5.1
install:
  # Install Jekyll and dependencies
  - bundle install
  - npm install


script:
  - gulp
  - bundle exec rake site:deploy --quiet #--trace

branches:
  only:
  - source
