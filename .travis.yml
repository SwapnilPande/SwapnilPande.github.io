sudo: false
language: ruby
cache: bundler
rvm:
- 2.5.1
install:
  # Install Jekyll and dependencies
  - bundle install
  - npm install
  - npm install -g gulp-cli


script:
  - gulp
  - bundle exec rake site:deploy --quiet #--trace

branches:
  only:
  - source
